<!DOCTYPE html>
<html>
	<head>
		<meta charset="UTF-8">
		<title>
			{% block title %}{% endblock %}
			- Tables et fleurs d'un jour
		</title>
		<meta name="description" content="{% block description %}{% endblock %}">
		<meta
		name="robots" content="index, follow">


		{# google font #}
		<link rel="preconnect" href="https://fonts.googleapis.com">
		<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
		<link
		href="https://fonts.googleapis.com/css2?family=Pacifico&family=Poppins:ital,wght@0,100;0,200;0,300;0,400;0,500;0,600;0,700;0,800;0,900;1,100;1,200;1,300;1,400;1,500;1,600;1,700;1,800;1,900&display=swap" rel="stylesheet">

		{# favicon #}
		<link rel="icon" type="image/png" href="/favicon-96x96.png" sizes="96x96"/>
		<link rel="icon" type="image/svg+xml" href="/favicon.svg"/>
		<link rel="shortcut icon" href="/favicon.ico"/>
		<link rel="apple-touch-icon" sizes="180x180" href="/apple-touch-icon.png"/>
		<link rel="manifest" href="/site.webmanifest"/>

		{% block stylesheets %}
			{{ encore_entry_link_tags('app') }}
		{% endblock %}

		{% block javascripts %}
			{{ encore_entry_script_tags('app') }}
		{% endblock %}
	</head>
	<body
		class="body-visitor">
		{# navbar #}

		<nav class="navbar navbar-expand-lg navbar-light bg-light">
			<a class="navbar-brand" href="{{path('app_visitor_welcome')}}"><img src="/assets/img/logo-TFJ-bgremoved.png" alt="logo de tables et fleurs d'un jour"></a>
			<button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
				<span class="navbar-toggler-icon"></span>
			</button>

			<div class="collapse navbar-collapse" id="navbarSupportedContent">
				<ul class="navbar-nav mr-auto d-flex justify-content-between">

					<li class="nav-item active">
						<a class="nav-link" href="{{path('app_visitor_welcome')}}">Accueil
						</a>
					</li>
					<li class="nav-item">
						<a class="nav-link" href="{{path('app_visitor_welcome')}}#about">A propos</a>
					</li>
					<li class="nav-item dropdown">
						<a class="nav-link dropdown-toggle" href="{{path('app_visitor_location_index')}}" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
							Location
						</a>
						<div class="dropdown-menu" aria-labelledby="navbarDropdown">
							<a class="dropdown-item" href="#">A</a>
							<a class="dropdown-item" href="#">B</a>
							<div class="dropdown-divider"></div>
							<a class="dropdown-item" href="#">C</a>
						</div>
					</li>
					<li class="nav-item">
						<a class="nav-link" href="{{path('app_visitor_contact_create')}}">Contact</a>
					</li>
				</li>
			</ul>
			<ul class="navbar-nav ms-auto">
				{% if app.user %}
					{% if is_granted('ROLE_ADMIN') %}
						<li class="nav-item">
							<a class="my-1 mx-lg-2 btn btn-outline-secondary shadow" href="{{path('app_admin_home')}}">Espace admin</a>
						</li>
					{% endif %}

					{% if is_granted('ROLE_USER') %}
						<li class="nav-item">
							<a class="my-1 mx-lg-2 btn btn-outline-secondary shadow" href="{{path('app_user_home')}}">Espace utilisateur</a>
						</li>
					{% endif %}

					<li class="nav-item">
						<a class="my-1 mx-lg-2 btn btn-secondary shadow" href="{{path('app_logout')}}">Déconnexion</a>
					</li>
				{% else %}
					<li class="nav-item">
						<a class="my-1 mx-lg-2 btn btn-outline-primary shadow" href="{{path('app_login')}}">Connexion</a>
					</li>
					<li class="nav-item">
						<a class="my-1 mx-lg-2 btn btn-primary shadow" href="{{path('app_register')}}">Inscription</a>
					</li>
				{% endif %}
			</ul>
		</nav>
		{# main #}
		<main>
			{% block main %}{% endblock %}
		</main>
		{# footer #}
		<footer class="text-center ">
			<span>
				© Copyright - Tables et Fleurs d'Un Jour</span>
		</footer>
	</body>
</html></body></html>
