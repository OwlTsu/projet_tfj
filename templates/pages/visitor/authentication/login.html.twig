<!DOCTYPE html>
<html lang="fr">
	<head>

		{# Encodage standard des caractères pour le web #}
		<meta
		charset="UTF-8">

		{# Minimum de Responsive Design à toujours mettre en place #}
		<meta
		name="viewport" content="width=device-width, initial-scale=1.0">

		{# Le titre de la page #}
		<title>Connexion - Tables et fleurs d'un jour</title>

		{# La description de la page #}
		<meta
		name="description" content="Connectez-vous a votre compte tables et fleur d'un jour">

		{# Autorisation aux robots des moteurs de recherche d'indexer et de suivre la page #}
		<meta
		name="robots" content="index, follow">

		{# Les mots clés de la page #}
		<meta
		name="keywords" content="Inscription, Blog, Jean-Dupont">

		{# Google font #}
		<link rel="preconnect" href="https://fonts.googleapis.com">
		<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
		<link
		href="https://fonts.googleapis.com/css2?family=Pacifico&family=Poppins:ital,wght@0,100;0,200;0,300;0,400;0,500;0,600;0,700;0,800;0,900;1,100;1,200;1,300;1,400;1,500;1,600;1,700;1,800;1,900&display=swap" rel="stylesheet">

		{# Lien css de Bootstrap 5 #}
		{% block stylesheets %}
			{{ encore_entry_link_tags('app') }}
		{% endblock %}

		{# Lien js de Bootstrap 5 #}
		{% block javascripts %}
			{{ encore_entry_script_tags('app') }}
		{% endblock %}

	</head>
	<body class="vh-100">
		<div class="container mt-4">
			<div
				class="row mb-3">

				{# Img #}
				<a href="{{path('app_visitor_welcome')}}" class="col-lg-6 d-flex align-items-center mt-sm-5 mt-lg-0">
					<img src="/assets/img/logo-TFJ-bgremoved.png" alt="logo tables et fleurs d'un jour de la page inscription" class="img-fluid mx-auto">
				</a>


				{# Form #}

				<div class="col-lg-5 vh-100 d-flex flex-column justify-content-center align-items-center ms-4">

					<h1 class="my-3">Connexion</h1>

					{% include 'components/alert.html.twig' %}

					{% if error %}
						<div class="alert alert-danger text-center">{{ error.messageKey|trans(error.messageData, 'security') }}</div>
					{% endif %}

					<form method="post" class="w-100 mt-3">

						<div class="mb-3">
							<label for="inputEmail">Email</label>
							<input type="email" value="{{ last_username }}" name="email" id="inputEmail" class="form-control" autocomplete="email" required autofocus>
						</div>

						<div class="mb-3">
							<label for="inputPassword">Mot de passe</label>
							<input type="password" name="password" id="inputPassword" class="form-control" autocomplete="current-password" required>
						</div>

						<div class="mb-3">
							<input type="hidden" name="_csrf_token" value="{{ csrf_token('authenticate') }}">
						</div>

						<div class="mb-3">
							<label>
								<input type="checkbox" name="_remember_me">
								Se souvenir de moi
							</label>
						</div>

						<div>
							<button formnovalidate class="btn btn-secondary w-100" type="submit">Connexion</button>
						</div>
					</form>

					<div class="mt-3 text-center">
						<p>
							<a href="{{path('app_forgot_password_request')}}">Mot de passe oublié?</a>
						</p>
						<p>Vous n'avez pas encore de compte?
							<a href="{{path('app_register')}}">Inscrivez-vous</a>
						</p>
						<p>
							{# <a href="{{path('app_visitor_welcome')}}">Retour à l'accueil</a> #}
						</p>
					</div>
				</div>
			</div>
		</div>
	</body>
</html>
